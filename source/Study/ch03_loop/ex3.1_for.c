#include <stdio.h>
/*반복문  : for(특정횟수 반복), 
            while(조건만족시 반복), 
            do~while(1회수행후 조건만족시 반복)*/ 
int main(void){
    for(int i=1 ; i<=10 ; i++){

        printf("%d. Hello, world\n", i);
    }
}